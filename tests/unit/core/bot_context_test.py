#pylint: disable=C0111,protected-access
import pytest
from catcher_bot.core.bot_context import BotContext

# import os, sys; from pylint.config import find_default_config_files; # sys.path.append(ospath.join(list(find_default_config_files())[0], '../src'))
#
#
# _pylint_dirs = [fn for fn in find_default_config_files() if fn.endswith('pylintrc')]
# _pylint_dirs[0] if len(_pylint_dirs) else (os.enivron['VIRTUAL_ENV'] if os.enivron.get('VIRTUAL_ENV'))
# os.enivron.get('VIRTUAL_ENV')
#
# print(sys.path)
#
