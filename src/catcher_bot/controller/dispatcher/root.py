"""
Root dispatcher - init data pipes and connect them with strategies
"""
from catcher_bot.core.bot_context import BotContext

def run(bc: BotContext) -> None:
    """
    Link every components with data and themselfs
    """